<h1>Benutzerübersicht</h1>

<table>
  <thead>
  <tr>
    <th>Benutzername</th>
    <th>E-Mail</th>
    <th>Rolle</th>
    <th>Aktionen</th>
  </tr>
  </thead>
  <tbody>
  <% @users.each do |user| %>
    <tr>
      <td><%= user.username %></td>
      <td><%= user.email_address %></td>
      <td><%= user.role %></td>
      <td><%= link_to "Bearbeiten", edit_admin_user_path(user) if policy(user).edit? %></td>
    </tr>
  <% end %>
  </tbody>
</table>
